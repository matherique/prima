version: '3.9'

services:
  database: 
    image: postgres
    environment:
      - POSTGRES_DB=prisma
      - POSTGRES_USER=prisma
      - POSTGRES_PASSWORD=kFMdvvxmvd
    ports: 
      - "5430:5432"

